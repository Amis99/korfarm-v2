# 일일독해 정독 순서 보정 인수인계 보고서

## 작업 요약

- 대상: `frontend/public/daily-reading/**/*.json`의 `intensive.timeline`
- 문제: 누락 문장 질문을 추가/보정한 일부 학습에서 문단 중심 내용 질문이 먼저 나오고, 뒤에 같은 문단의 문장 질문이 이어지는 순서 오류가 있었다.
- 보정 기준: 같은 문단 안에서는 모든 문장 하이라이트 질문을 먼저 배치하고, 그 문단의 중심 내용 질문을 마지막에 배치한다.
- `stepId`는 바꾸지 않고 timeline 배열 순서만 재정렬했다.

## 생성/수정 파일

- 생성: `scripts/reorder-daily-reading-intensive-timeline.mjs`
- 수정: `frontend/public/daily-reading/**/*.json` 중 정렬 변경이 필요한 공식 일일독해 JSON 2259개
- 생성 백업: `tmp/daily-reading-intensive-order-backup-2026-04-24T00-12-56-269Z/`
- 생성 리포트:
  - `tmp/daily-reading-intensive-order-report-local-2026-04-24T00-12-35-000Z.json`
  - `tmp/daily-reading-intensive-order-report-local-2026-04-24T00-12-56-269Z.json`
  - `tmp/daily-reading-intensive-order-report-local-2026-04-24T00-13-12-679Z.json`
  - `tmp/daily-reading-range-audit-local-2026-04-24T00-13-15-897Z.json`

## 검증 결과

```text
node --check scripts/reorder-daily-reading-intensive-timeline.mjs
PASS
```

```text
node scripts/reorder-daily-reading-intensive-timeline.mjs --source=local
totalItems=4380
changedItems=2259
issuesBeforeCount=2254
issuesAfterCount=0
```

```text
node scripts/reorder-daily-reading-intensive-timeline.mjs --source=local --write-local
totalItems=4380
changedItems=2259
issuesBeforeCount=2254
issuesAfterCount=0
localBackupDir=tmp/daily-reading-intensive-order-backup-2026-04-24T00-12-56-269Z
```

```text
node scripts/reorder-daily-reading-intensive-timeline.mjs --source=local
totalItems=4380
changedItems=0
issuesBeforeCount=0
issuesAfterCount=0
```

```text
node scripts/audit-daily-reading-ranges.mjs --source=local
totalContents=4380
issueContents=0
intensive missing sentence/highlight/order 관련 이슈=0
confirm 관련 이슈=0
structural=0
```

대표 확인 파일: `frontend/public/daily-reading/saussure1/114.json` (`[비문학] 소풍 도시락 고르기`)

- 1문단: 문장 질문 5개 뒤에 `1문단의 중심 내용은 무엇인가요?`
- 2문단: 문장 질문 5개 뒤에 `2문단의 중심 내용은 무엇인가요?`

## DB 반영 상태

이 세션에서는 DB 반영을 완료하지 못했다.

```text
node scripts/reorder-daily-reading-intensive-timeline.mjs --source=db
ERROR: EPERM: operation not permitted, open 'C:\Users\RENEWCOM PC\.ssh\korfarm-ec2.pem'
```

원인: 현재 Codex 샌드박스가 SSH 키 파일을 읽을 권한이 없다. 스크립트 로직 오류가 아니라 세션 권한 문제다.

권한이 있는 세션에서 실행할 명령:

```bash
node scripts/reorder-daily-reading-intensive-timeline.mjs --source=db
node scripts/reorder-daily-reading-intensive-timeline.mjs --source=db --apply-db
```

`--apply-db` 실행 시 스크립트가 DB 원본을 `tmp/daily-reading-intensive-order-db-backup-*.json`으로 먼저 백업한 뒤 변경분만 업데이트한다.

## 커밋/배포 주의

- 디자인 하네스는 이 작업과 무관하므로 커밋에 포함하지 않는다.
- 이전 문제 모달 클릭 전환 작업 파일도 이 작업 커밋에 섞지 않는다.
- DB만 정상 반영하면 프론트엔드 AWS 재배포는 필요 없다.
- 로컬 정적 JSON을 운영 배포물에도 반영해야 하는 경로라면, 해당 JSON 커밋/푸시 후 별도 프론트 배포가 필요할 수 있다.

권장 커밋 범위:

```text
scripts/reorder-daily-reading-intensive-timeline.mjs
frontend/public/daily-reading/**/*.json
일일독해_정독순서_인수인계_보고서.md
작업_점검_보고서.json
```

단, `frontend/public/daily-reading`에는 기존 staged/dirty 변경이 섞여 있을 수 있으므로, 커밋 전 반드시 `git diff --cached`와 `git diff`로 이번 순서 보정만 들어갔는지 확인한다.

