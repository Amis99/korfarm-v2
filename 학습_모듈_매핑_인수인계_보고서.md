# 학습 모듈 매핑 정규화 인수인계 보고서

작성 시각: 2026-04-24 08:00 KST

## 목적

학습 콘텐츠의 `contentType`과 실제 실행 엔진 `moduleKey` 매핑이 화면별로 흩어져 있어, 같은 콘텐츠가 관리자/업로드/학습 실행 화면에서 서로 다른 모듈로 연결될 수 있던 문제를 정리했다.

일일독해 영역 분류 정규화 작업과 디자인 하네스 작업은 이 보고서의 대상이 아니다.

## 커밋 대상 파일

아래 7개 파일만 이번 작업 커밋 대상으로 잡으면 된다.

```bash
frontend/src/constants/contentTypes.js
frontend/src/pages/LearningRunnerPage.jsx
frontend/src/pages/AdminContentPage.jsx
frontend/src/pages/AdminContentUploadPage.jsx
frontend/src/pages/AdminProPage.jsx
frontend/src/data/learning/learningTemplates.js
frontend/src/engine/core/EngineShell.jsx
```

현재 diff 통계:

```text
7 files changed, 182 insertions(+), 162 deletions(-)
```

## 핵심 변경 내용

`frontend/src/constants/contentTypes.js`

- `MODULE_KEY_BY_CONTENT_TYPE`, `normalizeModuleKey`, `resolveModuleKeyForContentType`를 추가해 공용 매핑을 한 곳으로 모았다.
- `choice_judgement`는 현재 등록 엔진 기준에 맞춰 `choice_analysis`로 정규화한다.
- `GRAMMAR_POS`는 실제 템플릿 페이로드가 `questions` 구조라 `worksheet_quiz`로 연결한다.
- `MORPHEME_ANALYSIS`는 `morpheme_analysis`로 연결하고, 표시 라벨/짧은 라벨도 형태소 분석 기준으로 바로잡았다.
- `BACKGROUND`, `PRO_BACKGROUND`는 `background_knowledge`로 연결한다.
- 카테고리 탭 매핑에도 배경지식/형태소/선택지 계열 alias를 보강했다.

`frontend/src/pages/LearningRunnerPage.jsx`

- 페이지 내부의 중복 매핑을 제거하고 공용 resolver를 사용한다.
- `contentType`이 배열인 경우와 `content.contentType` 내부 원본값까지 고려해 moduleKey를 결정한다.
- farm log 시작 요청에는 배열이 아닌 대표 contentType 문자열을 보낸다.

`frontend/src/pages/AdminContentPage.jsx`

- 오래된 로컬 `CONTENT_TYPE_TO_MODULE` 매핑을 제거했다.
- 콘텐츠 원본 contentType과 category 배열을 함께 넘겨 공용 resolver로 moduleKey를 산출한다.

`frontend/src/pages/AdminContentUploadPage.jsx`

- 업로드/미리보기/단건 import/일괄 import/수정 흐름에서 공용 resolver를 사용한다.
- 일일 퀴즈 기본 선택값을 `dailyQuiz:quiz:worksheet_quiz`에서 `dailyQuiz:quiz:daily_quiz`로 수정했다.
- 배경지식/프로 배경지식 드롭다운 값을 `background_knowledge`로 수정했다.
- `CONTENT_PDF_MODULE`을 `farm:content:study_content`로 수정했다.
- 파싱된 콘텐츠에 `contentType` 또는 `moduleKey`가 있으면 드롭다운 기본값이 이를 덮어쓰지 않게 조정했다.

`frontend/src/pages/AdminProPage.jsx`

- 오래된 로컬 매핑을 제거하고 공용 resolver를 사용한다.

`frontend/src/data/learning/learningTemplates.js`

- `farm_background`, `pro_background` 템플릿의 `moduleKey`를 `worksheet_quiz`에서 `background_knowledge`로 수정했다.

`frontend/src/engine/core/EngineShell.jsx`

- `normalizeModuleKey`를 적용해 엔진 실행 시 alias moduleKey를 정규화한다.
- 진행률 추정, 3배 seed 보상, 레이아웃 판단, 인쇄/요약 표시에서 정규화된 moduleKey를 사용한다.
- `choice_analysis` 계열이 선택지 분석 모듈 흐름에서 누락되지 않게 보강했다.

## 커밋하면 안 되는 것

아래는 이번 작업 커밋 대상이 아니다.

- `디자인_하네스/` 전체
- `frontend/public/daily-reading/**` 일일독해 데이터 대량 변경분
- `tmp/**` 임시 백업/대체 인덱스/대체 객체 저장소
- 다른 AI 세션이 작업 중인 기존 워크트리들

`작업_점검_보고서.json`과 이 인수인계 보고서는 기록용이다. 코드 배포만 우선이면 위 7개 소스 파일만 커밋하면 된다.

## 백업 위치

수정 전 백업:

```text
tmp/learning-module-mapping-backup-20260424-063723/
```

## 검증 결과

성공:

```bash
git diff --check -- frontend/src/constants/contentTypes.js frontend/src/pages/LearningRunnerPage.jsx frontend/src/pages/AdminContentPage.jsx frontend/src/pages/AdminContentUploadPage.jsx frontend/src/pages/AdminProPage.jsx frontend/src/data/learning/learningTemplates.js frontend/src/engine/core/EngineShell.jsx
```

- whitespace error 없음.

성공:

```text
contentType/moduleKey resolver 단위 확인: resolver ok
```

실패:

```bash
npm.cmd run build
```

- 이 세션에서는 Node child_process spawn이 `EPERM`으로 막혀 Vite/esbuild 실행이 중단됐다.
- 빌드 실패가 코드 오류로 확인된 것은 아니다.

실패:

```bash
eslint 대상 파일 확인
```

- 기존 파일의 unused/hook/unreachable 계열 오류 때문에 중단됐다.
- 이번 업로드 페이지 변경 자체만의 새 문법 오류로 확인된 것은 아니다.

## 이 세션에서 막힌 지점

이 세션은 권한/네트워크가 막혀 커밋, 푸시, AWS 배포를 완료하지 못했다.

- 새 worktree 생성 실패: `.git/worktrees` 쓰기 권한 없음.
- 일반 커밋 실패: `.git/index.lock` 생성 권한 없음.
- 일반/임시 인덱스 add 실패: `.git/objects` 쓰기 권한 없음.
- GitHub 푸시 실패: `github.com:443` 연결 실패.
- AWS 확인 실패: `sts.ap-northeast-2.amazonaws.com` 연결 실패.
- bash 배포 스크립트 실행 실패: Bash CreateInstance access denied.
- Vite build 실패: Node child_process spawn `EPERM`.

참고로 기존 `.git`을 쓰지 않는 대체 인덱스/대체 객체 저장소 방식으로 커밋 객체는 한 번 생성했다.

```text
commit: 2fb814ac8c7339133de418bbdc1c6b1ea6aba697
objects: tmp/learning-module-alt-objects-20260424-075348/
index: tmp/learning-module-alt-index-20260424-075348.index
```

이 커밋 객체에는 위 7개 파일만 들어 있다. 다만 기존 세션에서 정상 권한이 있다면 아래의 일반 커밋 절차로 다시 커밋하는 편이 더 단순하다.

## 기존 AI 세션에서 실행할 권장 절차

1. 먼저 커밋 대상이 정확한지 확인한다.

```bash
git diff --name-status -- frontend/src/constants/contentTypes.js frontend/src/pages/LearningRunnerPage.jsx frontend/src/pages/AdminContentPage.jsx frontend/src/pages/AdminContentUploadPage.jsx frontend/src/pages/AdminProPage.jsx frontend/src/data/learning/learningTemplates.js frontend/src/engine/core/EngineShell.jsx
```

2. 7개 파일만 스테이징한다.

```bash
git add -- frontend/src/constants/contentTypes.js frontend/src/pages/LearningRunnerPage.jsx frontend/src/pages/AdminContentPage.jsx frontend/src/pages/AdminContentUploadPage.jsx frontend/src/pages/AdminProPage.jsx frontend/src/data/learning/learningTemplates.js frontend/src/engine/core/EngineShell.jsx
```

3. 스테이징 파일이 7개뿐인지 확인한다.

```bash
git diff --cached --name-only
```

기대 목록:

```text
frontend/src/constants/contentTypes.js
frontend/src/data/learning/learningTemplates.js
frontend/src/engine/core/EngineShell.jsx
frontend/src/pages/AdminContentPage.jsx
frontend/src/pages/AdminContentUploadPage.jsx
frontend/src/pages/AdminProPage.jsx
frontend/src/pages/LearningRunnerPage.jsx
```

4. 커밋한다.

```bash
git commit -m "학습 모듈 매핑 정규화"
```

5. 푸시한다.

```bash
git push origin gh-pages
```

6. AWS 배포한다.

```powershell
$env:CF_DISTRIBUTION_ID='E29A5UX2VDFB4I'
bash scripts/deploy-frontend.sh
```

배포 스크립트가 하는 일:

```text
frontend에서 npm ci
frontend에서 npm run build
frontend/dist/assets/ -> s3://korfarm-frontend/assets/ sync
frontend/dist/ -> s3://korfarm-frontend/ sync, assets/* 및 test-pdfs/* 제외, --delete 적용
CloudFront distribution E29A5UX2VDFB4I invalidation /*
```

## 배포 후 확인 포인트

- 배경지식 콘텐츠가 `worksheet_quiz`가 아니라 `background_knowledge` 엔진으로 열린다.
- 프로 배경지식도 `background_knowledge` 엔진으로 열린다.
- 형태소 분석은 `morpheme_analysis` 엔진으로 열린다.
- 품사/문법 포지션 계열 `GRAMMAR_POS`는 현재 데이터 구조에 맞춰 `worksheet_quiz`로 열린다.
- 선택지 판별 `choice_judgement`는 현재 등록 엔진 기준상 `choice_analysis`로 정상 실행된다.
- 일일 퀴즈 업로드 기본값이 `daily_quiz`로 잡힌다.

