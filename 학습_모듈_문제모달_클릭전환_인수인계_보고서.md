# 학습 모듈 문제 모달 클릭 전환 인수인계 보고서

작성 시각: 2026-04-24 08:35 KST

## 목적

문제 모달을 쓰는 비문법 학습 모듈에서 `QuestionModal`이 자동으로 뜨지 않게 하고, 학생이 하이라이트/문제 영역을 클릭하거나 터치했을 때만 모달이 열리도록 수정했다.

문법 전용 모듈은 수정 대상에서 제외했다. 단, 사용자가 명시한 대로 `worksheet_quiz`는 문법 전용 모듈로 보지 않고 이번 수정 범위에 포함했다.

## 커밋 대상 파일

운영 반영에 필요한 핵심 코드/CSS 파일은 아래 7개다.

```text
frontend/src/engine/shared/CumulativeQuestionCard.jsx
frontend/src/engine/modules/WorksheetQuizModule.jsx
frontend/src/engine/modules/ReadingTrainingModule.jsx
frontend/src/engine/modules/DailyQuizModule.jsx
frontend/src/engine/modules/BackgroundModule.jsx
frontend/src/engine/modules/LogicModule.jsx
frontend/src/styles/learning-engine.css
```

기록까지 함께 보존하려면 아래 2개도 같은 커밋에 포함한다.

```text
작업_점검_보고서.json
학습_모듈_문제모달_클릭전환_인수인계_보고서.md
```

## 커밋하면 안 되는 것

현재 작업트리에는 다른 AI/다른 작업의 변경이 많이 섞여 있다. 아래는 이번 커밋에 넣으면 안 된다.

- `디자인_하네스/**`
- `frontend/public/daily-reading/**`
- `tmp/**`
- 이전 “학습 모듈 매핑 정규화” 작업 파일 7개
- 그 밖의 대량 JSON, PDF, HWP, 생성 산출물

특히 `디자인_하네스`는 다른 AI가 작업 중인 영역이므로 건드리거나 스테이징하지 않는다.

## 변경 요약

`frontend/src/engine/shared/CumulativeQuestionCard.jsx`

- 공통 누적 문제 카드가 활성화되자마자 `QuestionModal`을 띄우던 동작을 제거했다.
- 활성 카드에서 하이라이트가 있으면 하이라이트 클릭/터치 시에만 모달을 연다.
- 하이라이트가 없으면 발문, 지문, 보기, 문제 텍스트 영역 클릭/터치 시 모달을 연다.
- 키보드 접근을 위해 활성 카드에 `Enter`/`Space` 트리거를 추가했다.
- 모달 닫기 콜백을 연결했다.

`frontend/src/engine/modules/WorksheetQuizModule.jsx`

- `worksheet_quiz`는 요청대로 수정 범위에 포함했다.
- 일반 객관식/빈칸 문제 모달을 자동 노출하지 않고, 활성 문항 클릭 시 열리도록 변경했다.
- `highlight.ranges` 배열도 하이라이트 렌더링 대상으로 지원했다.
- 활성 빈칸이 있으면 빈칸 클릭/터치 시 모달을 연다.
- 하이라이트가 없는 문제는 문제 텍스트 클릭/터치 시 모달을 연다.
- 상단 안내 문구를 추가했다.

`frontend/src/engine/modules/ReadingTrainingModule.jsx`

- INTENSIVE 단계의 `QuestionModal` 자동 노출을 제거했다.
- 하이라이트가 있으면 하이라이트 클릭/터치 시 모달을 연다.
- 하이라이트가 없으면 읽기 지문 영역 클릭/터치 시 모달을 연다.
- 상단 안내 문구를 추가했다.
- RECALL/CONFIRM 단계는 이번 요청 범위가 아니므로 기존 방식 유지.

`frontend/src/engine/modules/DailyQuizModule.jsx`

- FILL_BLANKS 모달을 자동 노출하지 않고 활성 빈칸 클릭/터치 시 열리도록 변경했다.
- 일반 객관식은 공통 `CumulativeQuestionCard` 변경을 통해 클릭식 모달이 된다.
- TEXT_SELECT는 원래 텍스트 선택형 상호작용이라 공통 모달을 새로 붙이지 않았다.
- 상단 안내 문구를 추가했다.

`frontend/src/engine/modules/BackgroundModule.jsx`

- 누적 문제 카드 영역 상단에 “문제를 읽고 클릭하면 답안을 입력할 수 있습니다.” 안내 문구를 추가했다.
- 실제 모달 동작은 공통 `CumulativeQuestionCard` 변경을 따른다.

`frontend/src/engine/modules/LogicModule.jsx`

- 누적 문제 카드 영역 상단에 “문제를 읽고 클릭하면 답안을 입력할 수 있습니다.” 안내 문구를 추가했다.
- 실제 모달 동작은 공통 `CumulativeQuestionCard` 변경을 따른다.

`frontend/src/styles/learning-engine.css`

- `.learning-modal-instruction` 안내 문구 스타일 추가.
- `.modal-trigger-ready` 클릭 가능 상태와 focus-visible 스타일 추가.

## 문법 전용 모듈 제외 확인

아래 4개 문법 전용 모듈은 최종 diff가 없어야 한다.

```text
frontend/src/engine/modules/PhonemeChangeModule.jsx
frontend/src/engine/modules/WordFormationModule.jsx
frontend/src/engine/modules/SentenceStructureModule.jsx
frontend/src/engine/modules/MorphemeAnalysisModule.jsx
```

확인 명령:

```bash
git diff --name-only -- frontend/src/engine/modules/PhonemeChangeModule.jsx frontend/src/engine/modules/WordFormationModule.jsx frontend/src/engine/modules/SentenceStructureModule.jsx frontend/src/engine/modules/MorphemeAnalysisModule.jsx
```

기대 결과: 파일명 출력 없음.

## 현재 검증 결과

성공:

```bash
git diff --check -- frontend/src/engine/shared/CumulativeQuestionCard.jsx frontend/src/engine/modules/WorksheetQuizModule.jsx frontend/src/engine/modules/ReadingTrainingModule.jsx frontend/src/engine/modules/DailyQuizModule.jsx frontend/src/engine/modules/BackgroundModule.jsx frontend/src/engine/modules/LogicModule.jsx frontend/src/styles/learning-engine.css 작업_점검_보고서.json
```

- whitespace error 없음.

성공:

```bash
node -e "const fs=require('fs'); JSON.parse(fs.readFileSync('작업_점검_보고서.json','utf8')); console.log('report json ok')"
```

- `작업_점검_보고서.json` 파싱 정상.

실패:

```bash
npx.cmd eslint --quiet src/engine/shared/CumulativeQuestionCard.jsx src/engine/modules/WorksheetQuizModule.jsx src/engine/modules/ReadingTrainingModule.jsx src/engine/modules/DailyQuizModule.jsx src/engine/modules/BackgroundModule.jsx src/engine/modules/LogicModule.jsx
```

- 기존 코드의 unused 변수, React Compiler preserve-manual-memoization, set-state-in-effect 규칙 오류 때문에 실패한다.
- 이번 수정으로 생겼던 `CumulativeQuestionCard` hook-order 오류와 신규 모달 닫기 effect 관련 오류는 제거했다.

실패:

```bash
npm.cmd run build
```

- 이 세션의 로컬 환경에서는 `esbuild spawn EPERM` 때문에 Vite config 로드 단계에서 실패한다.
- 권한이 있는 배포 세션에서 반드시 다시 실행해야 한다.

## 권장 커밋 절차

1. 대상 파일 diff만 확인한다.

```bash
git diff --name-status -- frontend/src/engine/shared/CumulativeQuestionCard.jsx frontend/src/engine/modules/WorksheetQuizModule.jsx frontend/src/engine/modules/ReadingTrainingModule.jsx frontend/src/engine/modules/DailyQuizModule.jsx frontend/src/engine/modules/BackgroundModule.jsx frontend/src/engine/modules/LogicModule.jsx frontend/src/styles/learning-engine.css 작업_점검_보고서.json 학습_모듈_문제모달_클릭전환_인수인계_보고서.md
```

2. 이번 작업 파일만 스테이징한다.

```bash
git add -- frontend/src/engine/shared/CumulativeQuestionCard.jsx frontend/src/engine/modules/WorksheetQuizModule.jsx frontend/src/engine/modules/ReadingTrainingModule.jsx frontend/src/engine/modules/DailyQuizModule.jsx frontend/src/engine/modules/BackgroundModule.jsx frontend/src/engine/modules/LogicModule.jsx frontend/src/styles/learning-engine.css 작업_점검_보고서.json 학습_모듈_문제모달_클릭전환_인수인계_보고서.md
```

3. 스테이징 목록이 정확한지 확인한다.

```bash
git diff --cached --name-only
```

기대 목록:

```text
frontend/src/engine/modules/BackgroundModule.jsx
frontend/src/engine/modules/DailyQuizModule.jsx
frontend/src/engine/modules/LogicModule.jsx
frontend/src/engine/modules/ReadingTrainingModule.jsx
frontend/src/engine/modules/WorksheetQuizModule.jsx
frontend/src/engine/shared/CumulativeQuestionCard.jsx
frontend/src/styles/learning-engine.css
작업_점검_보고서.json
학습_모듈_문제모달_클릭전환_인수인계_보고서.md
```

4. 커밋한다.

```bash
git commit -m "학습 모듈 문제 모달 클릭 전환"
```

5. 푸시한다.

```bash
git push origin gh-pages
```

## AWS 배포 절차

이번 작업은 프론트엔드 코드 변경이므로 AWS 프론트엔드 배포가 필요하다.

권한이 있는 세션에서 먼저 빌드가 통과하는지 확인한다.

```bash
cd frontend
npm ci
npm run build
cd ..
```

빌드 통과 후 배포한다.

```powershell
$env:CF_DISTRIBUTION_ID='E29A5UX2VDFB4I'
bash scripts/deploy-frontend.sh
```

배포 스크립트가 수행하는 일:

```text
frontend에서 npm ci
frontend에서 npm run build
frontend/dist/assets/ -> s3://korfarm-frontend/assets/ sync
frontend/dist/ -> s3://korfarm-frontend/ sync, assets/* 및 test-pdfs/* 제외, --delete 적용
CloudFront distribution E29A5UX2VDFB4I invalidation /*
```

## 배포 후 스모크 테스트

AWS 배포 후 브라우저에서 아래를 확인한다.

- `worksheet_quiz`: 화면 진입 시 문제 모달이 자동으로 뜨지 않는다.
- `worksheet_quiz`: 하이라이트/활성 빈칸 클릭 시 모달이 열린다.
- `worksheet_quiz`: 하이라이트 없는 문제는 문제 텍스트 클릭 시 모달이 열린다.
- `reading_training` INTENSIVE: 화면 진입 시 모달이 자동으로 뜨지 않는다.
- `reading_training` INTENSIVE: 하이라이트가 있으면 하이라이트 클릭 시 모달이 열린다.
- `reading_training` INTENSIVE: 하이라이트가 없으면 지문 클릭 시 모달이 열린다.
- `daily_quiz` FILL_BLANKS: 활성 빈칸 클릭 시 모달이 열린다.
- `background_knowledge`, `logic_reasoning`: 문제 카드 클릭 시 모달이 열린다.
- 문법 전용 모듈 4개 동작이 바뀌지 않았는지 확인한다.

