/**
 * 학습 모듈 공통 피드백 타이밍 상수 (통일 정책)
 *
 * - 정답: 0.6초 (정답 표시 애니메이션 확인 시간)
 * - 오답 / 재시도 후 정답: 3초 (정답·해설 학습 시간)
 * - B 패턴 오답 재시도: 0.6초 후 같은 문제 재시도 가능
 *
 * 모든 학습 모듈에 동일하게 적용.
 */

export const FEEDBACK = {
  // A 패턴 (정답·오답 관계없이 다음 문제로 진행)
  A_CORRECT_ADVANCE_MS: 600,   // 정답 → 0.6초 후 다음 (정답 애니메이션 확인)
  A_WRONG_ADVANCE_MS: 3000,    // 오답 → 정답 선택지 3초 표시 후 다음

  // B 패턴 (정답을 맞힐 때까지 같은 문제)
  B_WRONG_RETRY_MS: 600,           // 오답 → 0.6초 후 재시도 가능
  B_CORRECT_FIRST_MS: 600,         // 첫 시도 정답 → 0.6초 후 다음
  B_CORRECT_FINAL_MS: 3000,        // 재시도 후 정답 → 정답 표시 3초 후 다음 (legacy 명칭, B_CORRECT_RETRY_MS와 동등)
  B_CORRECT_RETRY_MS: 3000,        // 재시도 후 정답 → 정답 표시 3초 후 다음 (의미 명확)
};
