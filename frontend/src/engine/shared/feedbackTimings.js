/**
 * 학습 모듈 공통 피드백 타이밍 상수
 *
 * 두 가지 패턴:
 *
 * A 패턴 — 정답·오답 관계없이 다음 문제로 진행
 *   - 정답: 즉시(딜레이 0) → 다음
 *   - 오답: 정답 선택지 표시 후 3초 → 다음
 *
 * B 패턴 — 정답을 맞힐 때까지 다음으로 안 넘어감
 *   - 첫 시도 정답: 즉시(딜레이 0) → 다음
 *   - 첫 시도 오답: 고른 선택지 사라짐(disabled) → 같은 문제 재시도
 *   - 재시도 후 정답: 정답 표시 후 3초 → 다음
 */

export const FEEDBACK = {
  // A 패턴
  A_CORRECT_ADVANCE_MS: 0,     // 정답 → 즉시 다음 (딜레이 없음)
  A_WRONG_ADVANCE_MS: 3000,    // 오답 → 정답 선택지 3초 표시 후 다음

  // B 패턴
  B_WRONG_RETRY_MS: 600,           // 오답 → 짧은 피드백 후 재시도 가능
  B_CORRECT_FIRST_MS: 0,           // 첫 시도 정답 → 즉시 다음 (딜레이 없음)
  B_CORRECT_FINAL_MS: 3000,        // 재시도 후 정답 → 정답 표시 3초 후 다음 (legacy 명칭, B_CORRECT_RETRY_MS와 동등)
  B_CORRECT_RETRY_MS: 3000,        // 재시도 후 정답 → 정답 표시 3초 후 다음 (의미 명확)
};
